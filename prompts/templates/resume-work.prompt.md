# Template: resume work

Use this prompt to resume an interrupted workflow deterministically.

```
Read `AGENTS.md`, then `decomp-docs/README.md` and `decomp-docs/workflow_manifest.json`.
Recompute the next ready tool and step from repository state.
Apply `decomp-docs/state_recovery.md` before trusting prior evidence.
Load the matching generic step doc and per-tool step doc.
Use `confirmed`, `open`, and `blocked` statuses consistently.
Continue implementation work until closeout criteria or an explicit reproducible blocker.
```
