# Template: step handoff

Use this prompt to hand work from one agent/session to another.

```
Read `AGENTS.md`, then canonical routing docs in `decomp-docs/`.
Select and state the active `<tool>` and step from repository artifacts.
List exact files read and modified.
Summarize parity evidence added and unresolved items.
State whether any previously captured evidence was marked stale and refreshed.
For unresolved items, include blocker, parity impact, and closure experiment.
Use only `confirmed`, `open`, `blocked` status terms.
```
