# Template: blocker triage

Use this prompt when a parity or implementation blocker appears.

```
Read `AGENTS.md` and `agents/runbooks/blocker-handling.md`.
Describe the blocker using repository evidence only.
State parity impact and current status (`blocked`).
Define the next closure experiment with command/fixture expectations.
If related evidence is stale, include required refresh actions in the experiment.
Update touched repository artifact with findings and status transitions.
```
